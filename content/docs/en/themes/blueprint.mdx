---
title: "Blueprint"
description: "Dark slate with cyan accent, monospace headings, technical precision."
---

import { blueprintTheme } from "@/registry/themes";

<ComponentPreview
  base="forme"
  name="invoice-classic"
  theme={blueprintTheme}
  hideCode
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @pdfcn/theme-blueprint
```

</TabsContent>
<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/themes/blueprint.ts"
  title="lib/themes/blueprint.ts"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { PdfcnThemeProvider } from "@/components/pdf/theme-provider";
import { blueprintTheme } from "@/lib/pdf-themes/blueprint";

export function Invoice() {
  return (
    <PdfcnThemeProvider theme={blueprintTheme}>
      {/* Your PDF content */}
    </PdfcnThemeProvider>
  );
}
```
